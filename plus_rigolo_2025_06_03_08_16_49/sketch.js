function setup() {
  createCanvas(400, 400);
  background(220);
  
  let x = 0;

  //Infinite loop : conidition is always true
  while( true ){
  circle ( x, height/2, 20);
  x += 50;
}
  
}

function draw() {


}